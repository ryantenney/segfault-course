// Exercise 06: Extern Variables — definitions
//
// TODO:
//   1. After adding `extern` to the declarations in ex06.h, include
//      the header here.
//   2. Define the variables with their correct values:
//        int max_connections = 100;
//        double pi = 3.14159265358979;
//        const char* app_name = "SegfaultCourse";
//   3. Implement init_config() — it can be empty since the variables
//      are initialized at their definitions.

#include <testkit/testkit.h>

// Called by the tests to verify the setup is complete.
// TODO: Implement (can be an empty body once variables are defined above).
void init_config() {
    TODO();
}
